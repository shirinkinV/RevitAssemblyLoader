<UserControl
    x:Class="RevitAssemblyLoader.UI.View.PluginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:RevitAssemblyLoader.UI.View"
    mc:Ignorable="d"
    d:DesignHeight="50"
    d:DesignWidth="300"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:vm="clr-namespace:RevitAssemblyLoader.UI.VM"
    d:DataContext="{d:DesignInstance Type=vm:PluginVM}">
    <StackPanel
        Orientation="Horizontal"
        Margin="10">
        <md:PackIcon
            Kind="Play"
            Margin="0 0 10 0"
            VerticalAlignment="Center" />
        <TextBlock
            Text="{Binding PluginAssemblyName}"
            VerticalAlignment="Center" />
    </StackPanel>
</UserControl>
